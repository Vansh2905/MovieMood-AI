# MovieMood-AI
MovieMood AI is a deep learning project that uses a Recurrent Neural Network (RNN) to analyze IMDb movie reviews and classify them as positive or negative. Built with PyTorch, it demonstrates NLP techniques including text preprocessing, tokenization, sequence modeling, and sentiment prediction.
